@extends('layouts.app') @section('title','Aprobaciones') @section('content')<div class="breadcrumb">ALMACÉN › Aprobaciones</div><div class="heading"><div><h1>Aprobaciones</h1><p>Aprueba o rechaza requerimientos con trazabilidad en MySQL.</p></div></div><div class="card"><div class="table-wrap"><table><thead><tr><th>Código</th><th>Fecha</th><th>Responsable</th><th>Proyecto</th><th>Ítems</th><th>Estado</th><th>Fecha de aprobación</th><th>Decisión</th></tr></thead><tbody>@foreach($requirements as $item)<tr><td><code>{{ $item->code }}</code></td><td>{{ $item->requested_at->format('d/m/Y') }}</td><td>{{ $item->responsible }}</td><td><strong>{{ $item->project }}</strong></td><td>{{ $item->items->count() }}</td><td><span class="badge {{ strtolower($item->status) }}">{{ $item->status }}</span></td><td>{{ $item->decision_at?->format('d/m/Y H:i') ?? 'Pendiente' }}</td><td><div class="decision"><form method="post" action="{{ route('approvals.decide',$item) }}">@csrf<input type="hidden" name="status" value="Aprobado"><button class="approve" {{ $item->status==='Aprobado'?'disabled':'' }}>✓ Aprobar</button></form><form method="post" action="{{ route('approvals.decide',$item) }}">@csrf<input type="hidden" name="status" value="Rechazado"><button class="reject" {{ $item->status==='Rechazado'?'disabled':'' }}>× Rechazar</button></form></div></td></tr>@endforeach</tbody></table></div>{{ $requirements->links() }}</div>@endsection
